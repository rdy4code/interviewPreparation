// Bit counting programming codewars

// precondition : receive positive integer
// postcondition: return number of bits equal to 1
var countBits = function(n) {
    return result = n.toString(2).split("0").join("").length
  };
